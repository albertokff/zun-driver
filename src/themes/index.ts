/*
========================================================
EXPORTAÇÃO CENTRAL DO TEMA.
========================================================
*/

import { colors, darkColors, lightColors } from "./colors";
import { typography } from "./typography";
import { spacing } from "./spacing";

export const theme = {
    colors,
    darkColors,
    lightColors,
    typography,
    spacing,
};